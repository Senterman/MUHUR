<?xml version="1.0" encoding="utf-8"?>
<!--
    MÜHÜR — AndroidManifest.xml
    Native Activity konfigürasyonu.
    Minimum SDK: 24 (Android 7.0) — EGL/GLES2 garantili
    Target  SDK: 34 (Android 14)
-->
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.muhur.game"
    android:versionCode="1"
    android:versionName="0.1.0">

    <!-- ── Permissions ─────────────────────────────────── -->
    <!-- İleride: INTERNET (skor tablosu), VIBRATE (kart haptiği) -->

    <!-- ── SDK Hedefleri ───────────────────────────────── -->
    <uses-sdk
        android:minSdkVersion="24"
        android:targetSdkVersion="34" />

    <!-- ── Donanım Gereksinimleri ──────────────────────── -->
    <uses-feature android:glEsVersion="0x00020000" android:required="true" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="true" />

    <!-- ── Uygulama ────────────────────────────────────── -->
    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:hasCode="false"
        android:allowBackup="false"
        android:supportsRtl="false">

        <!--
            NativeActivity:
            - android:name → Android'in standart NativeActivity sınıfı
            - android.app.lib_name → yüklenecek .so adı (lib prefix olmadan)
            - configChanges → rotation/resize'da Activity yeniden başlamaz,
              onAppCmd ile C++ tarafı kendi handle eder
        -->
        <activity
            android:name="android.app.NativeActivity"
            android:label="@string/app_name"
            android:configChanges="orientation|keyboardHidden|screenSize|screenLayout"
            android:screenOrientation="portrait"
            android:exported="true"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">

            <!-- Hangi .so yüklensin -->
            <meta-data
                android:name="android.app.lib_name"
                android:value="muhur" />

            <!-- Launcher Activity -->
            <intent-filter>
                <action   android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>
</manifest>
